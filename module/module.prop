id=pico4_swift_force_enable
name=PICO 4 Motion Tracker Unlock (2.0.5)
version=v2.6
versionCode=11
author=wzy
description=PICO 4 Motion Tracker 2.0.5 unlock for PICO 4 standard (ROM 5.13.x). v2.6 FIXED: tracker detection restored (native compat shim for missing getSwiftTrackerInfoVector), clean install/upgrade/uninstall (PackageManager cache handling), legacy residue cleanup. Stock APK, systemless. Requires root (Magisk). Reboot after install.
